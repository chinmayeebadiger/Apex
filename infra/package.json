{
  "name": "infra",
  "version": "0.1.0",
  "scripts": {
    "build": "tsc",
    "build:layer": "bash ../scripts/build-sandbox-layer.sh",
    "deploy": "npm run build:layer && npm run build && cdk deploy SandboxStack InfraStack",
    "watch": "tsc -w",
    "test": "jest",
    "cdk": "cdk"
  },
  "devDependencies": {
    "@types/jest": "^30",
    "@types/node": "^24.10.1",
    "aws-cdk": "2.1126.0",
    "esbuild": "^0.28.1",
    "jest": "^30",
    "ts-jest": "^29",
    "ts-node": "^10.9.2",
    "typescript": "~5.9.3"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.104.2",
    "@aws-sdk/client-lambda": "^3.1069.0",
    "@aws-sdk/client-secrets-manager": "^3.1069.0",
    "aws-cdk-lib": "^2.257.0",
    "constructs": "^10.5.0"
  }
}
